apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - namespace.yaml
  - certificate.yaml
  - ingress.yaml
namespace: podinfo-prod
configMapGenerator:
  - name: podinfo-values
    namespace: podinfo-prod
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
  immutable: true